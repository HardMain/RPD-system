# LLM Configuration (optional — works in demo mode without these)
LLM_API_KEY=demo
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# Security
SECRET_KEY=change-this-in-production-to-random-string

# Database (only change if not using docker-compose)
# DATABASE_URL=postgresql+asyncpg://rpd_user:rpd_secret@db:5432/rpd_db
